package alepando.dev.dialogapi.executor

/**
 * Default namespace used by the original `Keys` enum.
 * This can serve as an example or a default for plugins registering keys
 * if they don't have a specific namespace in mind, though using their own plugin ID
 * as the namespace is generally recommended for [CustomKeyRegistry].
 */
const val namespace = "dialog_plugin"
